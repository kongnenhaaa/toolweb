# Vercel server-side secrets. Never commit the real values.
FIREFOX_TOKEN=
FIREBASE_DATABASE_URL=https://toolweb-c7702-default-rtdb.firebaseio.com
FIREBASE_SERVICE_ACCOUNT_JSON={}
# Alternative that avoids multiline/whitespace issues in Vercel:
# FIREBASE_SERVICE_ACCOUNT_JSON_BASE64=

# Optional. Same-origin Vercel deployment does not need this.
# FIREBASE_ALLOWED_ORIGINS=https://your-frontend.example.com
